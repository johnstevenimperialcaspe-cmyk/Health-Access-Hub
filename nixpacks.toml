# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'Monorepo - configure services separately'"]

[start]
cmd = "echo 'This is a monorepo. Deploy backend and frontend as separate services.'"
